import { TSizes, TTypes } from "@/lib/types";

export type TProductUIProps = {
    title: string;
    article: number;
    images: string[];
    prise: number;
    sizes: number[];
    categories: string;
    model: string;
    season: string;
    color: string;
    description: string;
    about: string;
}